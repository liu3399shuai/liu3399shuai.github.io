var global = this

;(function() {

  var _js_clslist = {};
  var _js_methods = {};
  
  global.defineClass = function(declaration, properties, instMethods, clsMethods) {
  
      // 做一些JS层面的逻辑，过滤，转换
  
      // 把结果转到OC
      var result = _oc_defineClass(declaration, properties, instMethods, clsMethods);
      return result;
  }
  
  global.grayColor() function(){
      // 做一些JS层面的逻辑，过滤，转换
  
      // 把结果转到OC
      var result = _oc_callSelector(cls,instance,method,isInstance);
      return result;
  }
  
  global.share() function(){
      // 做一些JS层面的逻辑，过滤，转换
  
      // 把结果转到OC
      var result = _oc_callSelector(cls,instance,method,isInstance);
      return result;
  }
  
  global.isLogin() function(){
      // 做一些JS层面的逻辑，过滤，转换
  
      // 把结果转到OC
      var result = _oc_callSelector(cls,instance,method,isInstance);
      return result;
  }
  
})()
